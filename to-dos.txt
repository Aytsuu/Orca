1. Replace the mocked AI activity panel in ChatView with
     live reads of:
      - /agents/status
      - /agents/artifacts/latest
      - latest proposal endpoint

2. Replace static PlanView pending changes with backend
    proposal data.

3. Add transcript-capable ingestion before claiming file/
    image/audio support:
    - extend uploaded_file or add a companion table for
    extracted text

    - let ContextBuilder._get_files() return transcript
    text

    - broaden citation semantics so monitor/analyzer can
    cite file transcript chunks, not only chat_message
    IDs

4. If you want file-triggered runs before transcript
    support, either:
    - suppress monitor/analyzer output for file-only runs,
    or

    - create synthetic transcript-backed message records /
    citation IDs


Prompt this to AI after fixing the issues:
The process I have in mind is that actionable suggestions from planner should not be automatically added to pending changes. 
Instead, add a "move to plan" button that user can click to move the suggestion to pending changes.  
Meaning, AI Activity panel will serve as the initial gate of plan changes. 
Then user can review pending changes which are visualized in the plan tab before accepting or rejecting them.

